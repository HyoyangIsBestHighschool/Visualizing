너는 지금 여러 명이 함께 만드는 "교과 개념 시각화 플랫폼" 프로젝트의
일부를 개발하는 걸 도와줘야 해. 이 프로젝트는 이미 팀에서 만든
공용 틀(HTML/CSS/JS)이 있고, 나는 그중 내가 맡은 시뮬레이션만
구현하면 돼. 아래 규칙과 정보를 반드시 지켜서 작업해줘. 깃허브 링크는 아래와 같아.
https://github.com/HyoyangIsBestHighschool/Visualizing

## 1. 먼저 할 일
- 이 폴더의 README.md 파일을 읽고 프로젝트 구조를 파악해줘.
- 그다음 아래에서 내가 알려주는 파일 두 개만 열어서 확인해줘.

## 2. 내가 맡은 기능
- 과목/분야: [예: 수학 - 미적분]
- 시뮬레이션 이름: [예: 매개변수와 곡선]
- 구현하고 싶은 기능 설명 :
  [예: 사용자가 슬라이더로 t 값(0~2π)을 조정하면, 화면에 x(t), y(t)로
  정의된 곡선 위의 점이 실시간으로 움직이고, 그 점까지의 곡선 길이가
  숫자로 표시되게 하고 싶어. x(t) = cos(t), y(t) = sin(2t) 같은
  기본 함수를 예시로 넣어줘.]

## 3. 절대 지켜야 하는 규칙
- 내가 구현해야 하는 기능에 해당하는 파일들만 수정해.
  그 외 다른 파일(style.css, main.js, index.html, math.html, physics.html,
  다른 사람의 시뮬레이션 파일)은 절대 건드리지 마.
- html 파일 안에 이미 있는 아래 두 개의 id는 이름을 바꾸지 말고 그대로 사용해.
  - id="sim-stage" → 실제 시각화(그래프, 애니메이션, 캔버스 등)가 들어갈 자리
  - id="sim-controls" → 슬라이더, 버튼 등 조작 UI가 들어갈 자리
- 디자인은 style.css에 이미 정의된 색상/폰트를 그대로 활용해서 톤을 맞춰줘.
  직접 새로운 색상 값이나 폰트를 임의로 추가하지 마.
  (참고로 수학 파트는 청록색 계열, 물리학 파트는 호박색 계열 포인트 컬러를 써.)
- 새로운 라이브러리(외부 npm 패키지 등)를 추가해야 할 것 같으면,
  먼저 나에게 어떤 라이브러리를 왜 쓰고 싶은지 물어보고 진행해.
- 지금 단계에서는 빌드 도구(webpack, vite 등) 없이 순수 HTML/CSS/JS로만
  동작해야 해. GitHub Pages에 그대로 올라가는 정적 사이트이기 때문이야.

## 4. 작업 순서
1. 지정된 html, js 파일을 읽고 현재 어떤 자리(placeholder)가 있는지 확인해.
2. #sim-stage 안의 placeholder를 실제 시각화 코드로 교체해.
3. #sim-controls 안의 가짜 슬라이더/버튼을 실제 동작하는 컨트롤로 교체해.
4. js 파일에서 그 컨트롤과 시각화를 연결해서 실제로 동작하게 만들어.
5. 다 만들고 나면, 브라우저로 직접 열어서 잘 동작하는지 확인해줘.
6. 마지막에 내가 이해할 수 있도록, 어떤 파일을 어떻게 바꿨는지
   한글로 간단히 요약해줘.

## 5. 막히면
- 혹시 공용 파일(style.css, main.js 등)을 꼭 수정해야만 기능 구현이
  가능한 상황이라면, 마음대로 고치지 말고 "이런 이유로 공용 파일 수정이
  필요합니다"라고 먼저 알려줘. 내가 팀에 확인해볼게.
