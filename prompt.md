너는 지금 여러 명이 함께 만드는 "교과 개념 시각화 플랫폼" 프로젝트의
일부를 개발하는 걸 도와줘야 해. 이 프로젝트는 이미 팀에서 만든
공용 틀(HTML/CSS/JS)이 있고, 나는 그중 내가 맡은 시뮬레이션 하나만
구현하면 돼. 지금 첨부한 파일은 README.md, 내가 담당한 html 파일,
그에 대응하는 js 파일, 그리고 전체 공용 style.css 파일이야.
너는 저장소 전체 폴더 구조에는 접근할 수 없으니, 아래 정보와
첨부된 파일 내용만으로 판단해서 작업해줘.

## 1. 먼저 할 일
- 첨부된 README.md를 읽고 프로젝트 구조와 규칙을 파악해줘.
- 첨부된 style.css를 읽고, 이미 정의된 CSS 변수(--accent-math,
  --accent-physics, --panel, --border 등)와 클래스(.panel,
  .stage-placeholder, .control-fake-slider, .btn-mini 등)를 확인해줘.
- 첨부된 html 파일을 읽고, 이미 있는 #sim-stage / #sim-controls
  자리와 그 안의 placeholder 내용을 확인해줘.

## 2. 내가 맡은 기능
- 과목/분야: [예: 수학 - 미적분]
- 시뮬레이션 이름: [예: 매개변수와 곡선]
- 수정할 html 파일 경로: [예: pages/math/parametric.html]
- 수정할 js 파일 경로: [예: assets/js/pages/parametric.js]
- 구현하고 싶은 기능 설명 (최대한 자세히 적어줘):
  [예: 사용자가 슬라이더로 t 값(0~2π)을 조정하면, 화면에 x(t), y(t)로
  정의된 곡선 위의 점이 실시간으로 움직이고, 그 점까지의 곡선 길이가
  숫자로 표시되게 하고 싶어. x(t) = cos(t), y(t) = sin(2t) 같은
  기본 함수를 예시로 넣어줘.]

## 3. 절대 지켜야 하는 규칙
- 지금 첨부한 html 파일 하나와 js 파일 하나, 이 두 개만 결과물로 만들어줘.
  style.css나 README.md는 참고만 하는 거고, 수정하지 마.
- html 파일 안에 이미 있는 아래 두 개의 id는 이름을 바꾸지 말고 그대로 사용해.
  - id="sim-stage" → 실제 시각화(그래프, 애니메이션, 캔버스 등)가 들어갈 자리
  - id="sim-controls" → 슬라이더, 버튼 등 조작 UI가 들어갈 자리
- 새로운 색상 값이나 폰트를 직접 만들지 말고, style.css에 이미 있는
  CSS 변수와 클래스를 최대한 재사용해줘. (수학 파트는 청록색 계열,
  물리학 파트는 호박색 계열 포인트 컬러를 써.)
- 새로운 라이브러리(외부 npm 패키지, CDN 스크립트 등)를 추가해야 할 것
  같으면, 먼저 나에게 어떤 라이브러리를 왜 쓰고 싶은지 물어보고 진행해.
- 빌드 도구(webpack, vite, React 등) 없이 순수 HTML/CSS/JS로만 동작해야
  해. GitHub Pages에 그대로 올라가는 정적 사이트이기 때문이야.
- 이미지, 폰트 등 외부 파일을 새로 추가하지 말고, 가능하면 SVG나
  Canvas로 직접 그려줘.

## 4. 결과물 요청 형식
- 완성된 html 파일 전체 내용을 그대로 보여줘 (일부만 보여주지 말고).
- 완성된 js 파일 전체 내용을 그대로 보여줘.
- 마지막에 내가 이해할 수 있도록, 어떤 부분을 어떻게 구현했는지
  한글로 3~5줄 정도 요약해줘.
- 내가 파일을 로컬에 저장하고 브라우저로 열어봤을 때 확인해야 할
  체크포인트(예: "슬라이더를 움직이면 곡선이 따라 그려지는지 확인")도
  같이 알려줘.

## 5. 막히면
- 혹시 style.css나 README.md, 다른 파일 수정이 꼭 필요한 상황이라면,
  마음대로 고치지 말고 "이런 이유로 다른 파일 수정이 필요합니다"라고
  먼저 알려줘. 내가 팀에 확인해볼게.
